# 智能睡眠枕 App 汇报流程

## 一、电脑上打开虚拟手机界面

推荐用 Android Studio 打开：

1. 打开 Android Studio。
2. 选择 `Open`，打开项目目录：`D:\APP`。
3. 等右下角 Gradle 同步完成。
4. 在右上角设备选择框选择 `Pixel_6`。
5. 如果模拟器还没打开，点右侧 `Device Manager`，找到 `Pixel_6`，点击播放按钮启动。
6. 模拟器启动后，点击 Android Studio 顶部绿色三角形 `Run`。
7. 等 App 自动安装到虚拟手机里，就能在电脑上看到手机界面。

备用方式：

如果 Android Studio 里找不到设备，可以到右侧 `Device Manager` 查看 `Pixel_6` 是否存在；如果不存在，就重新创建一个 Pixel 系列设备，系统镜像选已下载的 Android 镜像。

## 二、建议汇报时长

建议控制在 6-8 分钟。

整体逻辑：

1. 先讲项目背景和目标。
2. 再展示 App 的主要页面。
3. 然后强调这一版根据反馈做了哪些调整。
4. 最后说明后续会接入服务器和硬件。

## 三、开场介绍

可以这样讲：

本项目是一个面向睡眠干预实验的智能睡眠枕 App 原型。当前版本主要完成了移动端交互骨架，包括用户登录、设备状态展示、睡眠记录、助眠控制、问卷入口、报告查看和基础设置。因为第一版硬件暂时还不能由 App 自主调节超声模式，所以 App 端先采用记录和提醒的方式，为后续硬件联动和服务器同步预留接口。

## 四、演示路线

### 1. 登录页

演示内容：

- 密码登录。
- 验证码登录。
- 说明后续账号会用于设备绑定和睡眠记录同步。

讲解重点：

这一页解决用户进入 App 的问题。除了密码登录，也预留了短信验证码登录，避免用户忘记密码后无法继续使用。

### 2. 首页

演示内容：

- 顶部设备状态。
- 蓝牙、电量、Wi-Fi、采集状态。
- 今日问卷入口。
- 昨夜睡眠摘要：睡眠时长、睡眠效率、入睡时间、醒来时间。
- 开始睡眠按钮。

讲解重点：

首页是用户每天使用频率最高的页面，所以这里突出两个动作：开始睡眠和查看昨夜结果。异常情况也会在首页提醒，比如电量不足、Wi-Fi 失败等。

### 3. 开始睡眠

演示内容：

- 点击 `开始睡眠`。
- 展示确认弹窗。
- 确认后进入实时监测页。
- 观察睡眠计时开始。

讲解重点：

开始睡眠属于关键操作，所以加入确认弹窗，减少误触。开始睡眠后，App 会进入记录中状态；从后台返回或切回首页不会自动结束，只有点击结束睡眠才会结束记录。

### 4. 实时监测页

演示内容：

- 当前睡眠状态估计。
- 已运行时长。
- 实时心率和呼吸。
- 助眠状态。
- 结束睡眠按钮。

讲解重点：

这一页用于睡眠过程中的状态查看。目前数据是原型演示数据，后续会由睡眠枕传感器和算法结果提供。

### 5. 助眠页

演示内容：

- 超声助眠固定 30 分钟计时。
- 说明当前硬件通过实体开关启动。
- 音频助眠。
- 定时关闭、渐弱停止、循环播放。
- 白噪声收藏。
- 自定义音乐入口。

讲解重点：

第一版硬件还没有 App 端调节模式的能力，所以没有做复杂控制面板。App 先记录用户打开硬件开关后的 30 分钟过程，并提醒结束。未来硬件支持后，可以在这一页扩展为模式选择和强度调节。

### 6. 睡眠记录

演示内容：

- 在助眠页切到 `睡眠记录`。
- 展示日、周、月切换。
- 周/月视图用日历形式展示历史记录。
- 点击某一天，下面显示详细记录。
- 点击睡眠时间轴上的不同阶段，查看阶段信息。

讲解重点：

睡眠记录采用更直观的日历形式，方便用户回看历史数据。日视图中加入了睡眠阶段时间轴，可以看到浅睡、深睡、REM 和清醒片段。

### 7. 睡眠报告

演示内容：

- 从首页 `查看报告` 或记录页 `查看完整分析` 进入。
- 展示总睡眠、效率、夜间清醒。
- 展示睡眠阶段时间轴。
- 展示报告解读和周趋势。
- 展示助眠使用情况。

讲解重点：

报告不仅展示数字，也增加了解读说明，让用户知道这些指标代表什么，以及哪些波动需要关注。

### 8. 问卷中心

演示内容：

- PSQI 评分表。
- 睡前填写。
- 醒后填写。
- 问卷提醒时间。
- 标记完成/改为未填写。

讲解重点：

PSQI 在实验前后各填写一次，用于对比干预前后的睡眠质量。睡前和醒后问卷每天填写一次，用于补充主观体验。当前先接入问卷星链接，后续可以接服务器自动同步填写状态。

### 9. 设备页

演示内容：

- 设备连接状态。
- 蓝牙连接。
- Wi-Fi 连接。
- 固件版本。
- 最近同步。
- 超声模块说明。
- 固件升级和解除绑定确认弹窗。

讲解重点：

设备页负责硬件状态和维护操作。关键操作加入确认弹窗，比如升级固件和解除绑定，减少误操作。

### 10. 设置页

演示内容：

- 日间版/夜间版。
- 大字体。
- 长辈简洁模式。
- 作息目标设置。
- 演示/调试区域切换设备状态。

讲解重点：

考虑到睡眠问题在长辈群体中较常见，所以加入了可读性和适老化设置。演示/调试区域只是原型阶段用来模拟设备状态，后续会由真实硬件自动上报。

### 11. 结束睡眠

演示内容：

- 回到首页或实时监测页。
- 点击 `结束睡眠`。
- 展示确认弹窗。
- 确认后进入报告页。
- 说明助眠倒计时会同步停止。

讲解重点：

结束睡眠也是关键操作，所以同样需要确认。结束后会保存本次记录，并停止助眠倒计时，避免出现“睡眠结束了但助眠还在计时”的状态不一致。

## 五、已经根据反馈完成的优化

- 增加验证码登录。
- 关键操作增加确认弹窗。
- 首页增加设备异常提醒。
- 睡眠记录改成日/周/月和日历形式。
- 睡眠报告增加指标解读。
- 加入问卷星正式问卷入口。
- 加入睡前和醒后问卷提醒设置。
- 加入日间版/夜间版。
- 加入大字体和长辈简洁模式。
- 超声助眠暂时简化为 30 分钟固定计时，符合第一版硬件能力。
- 首页和实时监测页都可以结束睡眠。
- 结束睡眠时同步停止助眠倒计时。
- 适配手机底部系统栏，避免底部导航遮挡。

## 六、技术实现说明

可以这样讲：

当前 Android 版本采用“原生 Android 外壳 + HTML 原型页面”的方式实现。Android 负责 WebView 容器、权限、系统返回键、外部问卷链接、手机底部安全区适配，以及后续接 BLE 和服务器的入口。页面层负责快速迭代 UI 和交互逻辑。

后续如果接入真实设备，可以在 Android 的 `SleepPillowBleManager` 中实现蓝牙扫描、连接、发送 Wi-Fi 配置信息和读取实时数据。如果接入服务器，可以把睡眠记录、问卷完成状态、设备同步状态上传到后端。

## 七、后续计划

可以按这个顺序讲：

1. 接入真实睡眠枕蓝牙通信。
2. 接入服务器账号体系和睡眠记录接口。
3. 问卷星结果或问卷填写状态同步到服务器。
4. 完成真实心率、呼吸和睡眠阶段数据展示。
5. 优化报告算法，加入周/月趋势和异常波动提醒。
6. 等硬件支持后，扩展 App 端超声模式选择和强度控制。

## 八、结尾总结

可以这样收尾：

这一版 App 的重点不是一次性做完全部功能，而是先把用户每天会接触到的核心流程搭起来：登录、绑定设备、开始睡眠、助眠、记录、报告和问卷。当前原型已经能完整展示实验使用流程，也为后续服务器、蓝牙和真实硬件数据接入留好了位置。
